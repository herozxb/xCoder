# 1  I can train a LLM to decide which database to use of the 2 database
then I can train a LLM to decide which database to use of the N database

# 2  I can train a LLM to decide which word token to use of the 130k word token